# 接手 AI 必读：R08 智能戒指

执行设备操作前完整阅读 `HANDOFF.md`、`README.md` 和 `firmware_research/README.md`。不要重复已经完成的逆向，也不要把 OTA 应用包当作整机 Flash 备份。

## 当前目标与状态

- 设备：`R08_9C07`，MAC `31:31:45:37:9C:07`，硬件 `RT08_V3.1`。
- Device Information 固件字符串仍为 `RT08_3.10.48_260309`，但当前已经激活 v11 自定义固件。
- 当前 v11 SHA-256：`7b60058f5d4de8246834acf139b059009495e0dc9a811b5ff041ec33e3e00e0f`。v11 只在原厂合成队列标记 button=1 且 `abs(Y)>=16` 时输出滚轮，两套竖滑数组离线均为同向 2 格；144/144、DFU CHECK、DFU END 和重启后的 `A1 FA` 已成功，真实滑动手感仍待用户验收。DFU 入口仍只锁定该精确 v11，不能用于重刷 v10。
- v10 SHA-256：`6cd256de135ce4290794feebec808cdf4cea2e6fd9dfdd30e675a16fcb7927bb`；原生触控滚动、方向、无光标移动和无点击已真机确认。其逐帧 Y 符号转换会把前导校准/尾部样本也变成滚轮，导致一次滑动偶尔反向且速度偏快。
- v8/v9 通过 `A2 10` 输出约 9～10 Hz IMU；30 秒真机测试已确认双向滚动、回正停止、续期和短暂 BLE 空档恢复。
- 推荐滚轮参数：`--gain 0.2 --full-speed 60`。
- v8/旧固件保留主机 IMU 双击唤醒兜底：待机保持约 10 Hz IMU 流但不注入滚轮，两次独立敲击间隔 180～850 ms 时开启 60 秒控制。
- 只有校验正确的 v9 `A1 FC`、v10 `A1 FB` 或 v11 `A1 FA` 标记和 Windows Win32 GATT 后端同时成立时，组合模式才发送 `3B` 并使用电容触控双击。v10/v11 原生滚轮还要求显式 `--inject`。v8 `A1 FD` 路径禁止发送 `3B`，因为真机证明它会关闭当前 GATT 对象。
- v9 固件端短路三个属性索引 4 的 HID 鼠标报告入口；v10/v11 保留普通报告发送器但清零按钮/X/Y，只输出滚轮，并短路扩展报告入口。v11 的 button 是内部过滤条件，不会发送给主机。主机因此不再停用 HID 子设备。v8/旧固件仍必须精确匹配并停用 R08 HID 鼠标，或在系统未枚举该子设备时按 GATT-only 继续；枚举错误或停用失败仍 fail closed。
- 原厂 `0x1D` 仍只有离散点击/上下滑；左右滑和长按没有稳定动作码。

用户需要的是上下滚轮、双击复制、三击粘贴，以及可微调的连续姿态滚轮。不要把 IMU 姿态流描述成触摸表面的连续坐标。

## 每次调试

1. 先运行 `scripts\stop_r08_touch.bat`。
2. 只结束本项目遗留的 `r08.exe`；不要误杀无关程序。
3. 完全退出手机 QRing App并关闭手机蓝牙。
4. 确认只有一个控制程序会注入输入。
5. 输出必须保留可见控制台或写入被忽略的日志。

Windows 启动脚本应每次使用 Cargo 增量构建当前源码，不能只根据 `target\release\r08.exe` 是否存在来判断是否需要构建；分支切换后旧二进制可能缺少新子命令。

Windows 设置显示未连接不等于 GATT 不可用；程序优先复用系统的 Win32 GATT 连接。手机与 Windows 不得同时占用戒指。

## 运行时安全

- 调试模式默认不注入；只有显式 `--inject` 才控制滚轮。
- 750 ms 无数据必须先释放输入并急停；该阈值来自 WinRT 真机通知抖动，不能重新降到 250 ms，除非更换稳定传输路径。连续失败最多恢复两次，且等待 `sequence=0`；恢复后连续 10 个有效样本才可清零预算。
- `A1 09` 启停/续期的 Windows GATT 写入可能阻塞超过 1 秒；首包超时、tracker 和续期基准必须使用写入完成后的新时间戳，不能复用进入异步写入前的 `now_ms`。
- 保留固件 12 秒硬超时、主机 8 秒续期、首包 1.5 秒超时、最大滚轮速度和重力可信范围。
- 异常退出必须释放 Ctrl、鼠标按钮和所有注入状态。
- `A1 04 04` 会开启光学/传感器原始模式并使 LED 闪烁；它不是触控开关。停止命令是 `A1 02`。
- 官方触控命令是 `0x3B`，不要把 `0x2A` 状态通知当作启用命令。

## 固件写入规则

- 当前设备被用户指定为可牺牲测试设备，但这不是对所有未来二进制的永久授权。
- 每个新候选必须先生成最终文件、计算 SHA-256，并取得用户对该精确哈希的新授权。
- 写入前必须确认 `RT08_V3.1`、电量、连接互斥、候选大小、外层校验和、内层 SDK 摘要与独立激活标记。
- 不跨硬件版本刷包，不修改 Bootloader，不用未知命令探测，不把中断升级当作退出方式。
- 当前只证明应用仍能启动时可以覆盖恢复；没有独立恢复入口、整片备份或异常掉电恢复证明。

## 仓库规则

公开仓库只保留我们编写的源码、测试、协议结论和哈希。不要提交：

- 官方 APK、SDK、固件或候选二进制
- APK 反编译源码、第三方工具和参考仓库快照
- 账号信息、token、ADB 配对码、临时 IP/端口
- 手机系统日志、通知截图、BLE/HCI 完整抓包
- 本地构建产物、缓存、CSV、运行日志

核心回归：

```powershell
cargo fmt --all -- --check
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo build -p r08 --release --bins
python -m pytest -q firmware_research\scripts
```
