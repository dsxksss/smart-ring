# 接手 AI 必读：R08 智能戒指

请先完整阅读 `HANDOFF.md`、`README.md` 和 `firmware_research/README.md`，再执行任何设备操作。不要从头重复逆向，也不要把“蓝牙能连接”误当作“固件已经备份”。

## 用户真正要的效果

1. 戒指上下滑动控制 Windows 滚轮，而不是移动鼠标指针。
2. 双击复制、三击粘贴。
3. 希望滚动能够连续、灵敏并可微调；如果硬件没有连续触摸坐标，需要明确说明限制，不能把插值动画说成真实触摸跟踪。
4. 后续固件研究必须以备份和可恢复为前提，宁可停住也不能冒险把唯一设备刷坏。

## 已确认事实，不要重新猜测

- 目标：`R08_9C07`，MAC `31:31:45:37:9C:07`
- Hardware Revision：`RT08_V3.1`
- Firmware Revision：`RT08_3.10.48_260309`
- Windows 会把它枚举为 BLE HID 鼠标/用户控制设备。
- R08 固件把触控上下滑主要报告为 HID 相对 Y 位移，而不是 Wheel；程序负责转换成滚轮并抑制指针漂移。
- 自定义 GATT `0x1D` 通知是离散动作：`1` 点击、`2` 下滑、`3` 上滑。它不是逐点触摸流。
- HID 报告描述符没有绝对坐标、压力、接触面积或公开 Feature Report。不要承诺可以从现有公开 HID/GATT 中读取更多连续触控数据。
- 左滑、右滑、长按没有稳定独立动作码，不能可靠映射光标、退格和撤销。
- `A1 03` 加速度只是原始传感器通道之一；当前采样/上报速度不足以提供手指级滚轮微调。
- `A1 04 04` 会启动光学/传感器原始数据，导致 LED 持续闪烁并耗电；它不是触控开关。停止命令为 `A1 02`。
- 官方触控命令为 `0x3B`；不要再把 `0x2A` 状态通知当作触控启用命令。

## 开始调试前先检查残留

用户曾遇到关闭脚本后戒指仍影响电脑。每次开始前：

1. 运行 `stop_ring_touch.bat`。
2. 检查并结束本项目遗留的 `R08NativeCli`、`smart_ring_detector.py`、`record_r08_actions.py` 进程；不要误杀无关 Python/.NET 程序。
3. 确认只有一个控制程序会注入鼠标/键盘事件。
4. 调试输出必须保留可见控制台或写入日志，不能使用一闪而过、用户看不到错误的批处理窗口。

## 蓝牙连接互斥

- Windows 控制时：完全退出手机官方 App，并临时关闭手机蓝牙，避免手机抢占 BLE 连接。
- 手机 OTA/官方 App 调试时：先停止 Windows 控制程序，不要让 Windows 同时保持戒指 GATT/HID 会话。
- Windows“蓝牙设置连接不上”不代表程序无法使用。该设备同时涉及系统 HID 配对和应用级 BLE GATT，连接路径不同。
- ADB 无线调试端口和配对码是临时值。换电脑后重新配对，不要把旧端口/配对码写进代码或 GitHub。

## 滚轮体验的边界

- 自定义 `0x1D` 动作只能“一次动作滚一次”，Windows 端可以做平滑拆步，但不能知道手指在触控区的实时位置。
- 原生 Raw Input/HID 相对 Y 是目前更灵敏的输入源；优化时应以真实报告频率、增益、死区、反向切换和松手停止为指标。
- 戒指转动/加速度可以研究为独立手势，但现有约 1 Hz 原始传感器流、姿态漂移和 LED 副作用使其不适合作为精细连续滚轮。除非发现更高频且不启用光学 LED 的 IMU 通道，否则不要把它作为主方案。
- 任何“连续滚动”都必须有明确停止条件和最大速度，防止松手后继续滚动或出现鼠标按住状态。

## 键盘/鼠标注入注意事项

- 戒指输入必须按目标设备句柄/身份过滤，普通鼠标移动和点击不得参与戒指映射。
- 复制/粘贴注入后必须确保 Ctrl、鼠标按钮等全部释放，即使异常退出也不能留下按键按住状态。
- 三击的前两击与双击冲突。当前使用 850 ms 聚合窗口以容纳真机较慢三击；更改时必须同时测试双击延迟、三击识别率和误触。
- 不要默认启用会修改当前窗口内容的控制功能；调试模式默认关闭 Windows 注入。

## 固件研究硬红线

- 当前固件**尚未备份**。
- 版本号、APK、BLE 日志、HCI 抓包和 OTA 协议都不等于固件备份。
- 在 `firmware_research/README.md` 的防变砖清单全部满足前，不发送 DFU Start/Init/Data/Check/End，不进入擦除/升级模式，不写任何猜测命令。
- 不点击官方 App 的“确定升级/立即升级”；允许的只是正常账号下的版本查询和缓存下载。
- 只接受与 `RT08_V3.1` 精确匹配且来源可追溯的原厂镜像。取得后先做两份只读副本并分别计算 SHA-256。
- 不使用 APK 中的默认/硬编码 token 绕过 OTA 登录，不读取或上传用户账号 token。
- 没有确认 MCU、Flash 布局、签名/加密/反回滚和独立恢复路径前，不修改镜像。
- 拆机/测试点属于下一阶段；必须先取得用户明确授权、拍摄 PCB、确认电压，禁止盲接 5V。

## 隐私与仓库规则

该 GitHub 仓库当前是公开仓库。不要提交：

- 手机通知截图、个人聊天内容、账号信息
- ADB 配对码、临时 IP/端口
- Android bugreport、完整 HCI/系统日志
- 登录 token、默认 token 或请求头凭证
- 官方 APK、大段反编译源码或未经许可的固件镜像
- 本地构建产物和运行日志

可以提交经过脱敏的协议结论、我们自己编写的源码、测试、设备公开版本信息及原厂镜像的哈希/来源说明。

## 推荐继续顺序

1. 确认仓库干净，运行 14 项单元测试和原生 Release 构建。
2. 先解决当前 Windows 滚轮体验，使用日志量化 HID 报告频率和停止延迟。
3. 若继续固件研究，优先从官方 App 正常会话取得精确 OTA 缓存；没有镜像就停止在只读分析阶段。
4. 每次结束前停止控制进程、停止原始传感器模式，并记录做过的设备写命令（若有）。

